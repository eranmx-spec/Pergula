# עץ וצל — דף נחיתה

דף נחיתה לפרגולות, דקים, מעקות עץ וגדרות. כל פנייה מהטופס נכתבת כשורה חדשה
בגיליון Google Sheets בדרייב.

**הגיליון:** [לידים - פרגולות, דקים, מעקות וגדרות](https://docs.google.com/spreadsheets/d/1X851yBAkmOK_9Ot2pQ87wOxbtfpTXI_LiTva4icnFJI/edit)
נמצא בתיקיית `Pergula` בדרייב.

הטופס אוסף **שם מלא, טלפון ויישוב/כתובת** (שלושתם חובה), ובנוסף סוג הפרויקט
והערות חופשיות.

---

## שני שלבי הפעלה

הדף כבר עולה ועובד, אבל עד שלב 2 הוא **לא שולח נתונים** — לחיצה על "שליחה"
מציגה את מסך האישור ואומרת שזו תצוגה בלבד.

### שלב 1 — ההגדרות

כל ההגדרות של האתר יושבות בקובץ אחד: [`partials/config.js`](partials/config.js).
הוא מוזרק לשלושת העמודים בזמן הבנייה.

```js
window.PERGULA = {
  endpoint:         "PASTE_YOUR_APPS_SCRIPT_URL_HERE",
  whatsapp:         "972559272777",
  whatsappDisplay:  "055-927-2777",
  whatsappText:     "היי, הגעתי מהאתר ואשמח להצעת מחיר",
  a11yContactName:  "",
  a11yContactPhone: "",
  a11yContactEmail: ""
};
```

מספר הוואטסאפ כבר מוגדר. שלושת שדות `a11yContact*` הם פרטי רכז הנגישות,
והם **דרישה בתקנות** — ראו „נגישות" למטה.

### שלב 2 — חיבור הגיליון

1. פתחו את [הגיליון](https://docs.google.com/spreadsheets/d/1X851yBAkmOK_9Ot2pQ87wOxbtfpTXI_LiTva4icnFJI/edit)
   ובתפריט: **תוספים ← Apps Script**.
2. מחקו את מה שיש בעורך, והדביקו את כל התוכן של [`apps-script/Code.gs`](apps-script/Code.gs).
3. שמרו, ואז **Deploy ← New deployment**:
   - Select type: **Web app**
   - Execute as: **Me**
   - Who has access: **Anyone**  ← קריטי. בלי זה גולשים אנונימיים לא יוכלו לשלוח.
4. אשרו את ההרשאות (מסך "Google hasn't verified this app" ← Advanced ← Go to…).
5. העתיקו את ה-**Web app URL**. הוא נראה כך:
   `https://script.google.com/macros/s/AKfy…/exec`
6. הדביקו אותו כ-`endpoint` ב-`CONFIG`, ואז הריצו `bash build.sh` ודחפו:

```bash
bash build.sh
git commit -am "חיבור הגיליון"
git push
```

לבדיקה: פתחו את כתובת ה-Web App בדפדפן. אם חוזר `{"ok":true,…}` — היא חיה.
אחר כך שלחו פנייה אחת מהדף ובדקו שהשורה הופיעה בגיליון.

---

## עלייה לאוויר על pergula.online

הקוד כבר ב-`main`, וקובץ `CNAME` בשורש מצהיר על הדומיין. נשארו שני
שלבים ידניים, **בסדר הזה**.

### א. DNS ב-Namecheap

Domain List ← Manage ← **Advanced DNS**.

קודם **מוחקים** את שתי רשומות החנייה שקיימות שם כברירת מחדל — הן
מתנגשות עם GitHub:

| למחוק | |
|---|---|
| `A Record` | `@` → `192.64.119.72` |
| `CNAME Record` | `www` → `parkingpage.namecheap.com` |

אם יש גם `URL Redirect Record` על `@` — למחוק גם אותו.

ואז מוסיפים חמש רשומות:

| Type | Host | Value | TTL |
|---|---|---|---|
| A Record | `@` | `185.199.108.153` | Automatic |
| A Record | `@` | `185.199.109.153` | Automatic |
| A Record | `@` | `185.199.110.153` | Automatic |
| A Record | `@` | `185.199.111.153` | Automatic |
| CNAME Record | `www` | `eranmx-spec.github.io.` | Automatic |

ארבע כתובות ה-A הן שרתי GitHub Pages, וארבעתן נחוצות. ה-CNAME של
`www` גורם ל-`www.pergula.online` להפנות לדומיין הראשי.

### ב. הפעלת Pages ב-GitHub

**Settings ← Pages ← Source: „Deploy from a branch" ← Branch: `main` / `(root)` ← Save**

בשדה Custom domain אמור להופיע `pergula.online` מעצמו, מתוך קובץ ה-CNAME.

אחרי ש-GitHub מסיים לאמת את הדומיין, מסמנים **Enforce HTTPS**. התיבה
הזו אפורה עד שהתעודה מונפקת — זה יכול לקחת עד שעה מרגע שה-DNS מתעדכן,
וזה תקין.

מכאן והלאה כל `git push` ל-`main` מתעדכן באתר אוטומטית.

> ניסיתי להפעיל את Pages דרך GitHub Actions, אבל ה-`GITHUB_TOKEN` של
> workflow לא יכול להפעיל Pages בריפו שבו הוא מעולם לא הופעל — ולכן
> הצעד הזה חייב להיעשות פעם אחת מהממשק.

---

## מבנה

### קבצי מקור — כאן עורכים

| קובץ | תפקיד |
|---|---|
| `partials/config.js` | **כל ההגדרות.** טלפון, endpoint, רכז נגישות. |
| `page.body.html` | הדף הראשי: תוכן, עיצוב, האשף והטופס. |
| `docs/terms.body.html` | תוכן עמוד תנאי השימוש. |
| `docs/accessibility.body.html` | תוכן הצהרת הנגישות. |
| `partials/a11y-ui.html` | תפריט הנגישות וקישור הדילוג — משותף לכל העמודים. |
| `partials/a11y.js` | הלוגיקה של תפריט הנגישות. |
| `partials/wa-message.js` | נוסח הודעת הוואטסאפ: שורת מקור (עמוד וכפתור) וסיכום הפרטים אם הושארו. |
| `partials/wa-links.js` | מילוי קישורי וואטסאפ בעמודי המסמכים. |
| `apps-script/Code.gs` | הקוד שרץ בצד Google וכותב לגיליון. |
| `img/` | שתים-עשרה התמונות שבדף. |
| `pic/` | הקולאז'ים המקוריים. מקור בלבד. |
| `tools/crop-pics.ps1` | חתך את `pic/` ל-`img/`. הרצה חד-פעמית. |

### קבצים שנבנים — אל תערכו

`index.html`, `terms.html`, `accessibility.html` נדרסים בכל הרצה של
`build.sh`. **אחרי כל עריכה של קובץ מקור צריך להריץ `bash build.sh`
לפני commit**, אחרת האתר לא יתעדכן.

---

## אשף הפרויקט

לצד כל כפתור וואטסאפ יש כפתור „אשף הפרויקט". הוא שואל את הלקוח שאלות
לפי סוג העבודה (פרגולה, דק, מעקה או גדר — לכל אחד מסלול שאלות משלו),
ובסוף מרכיב סיכום ופותח את וואטסאפ עם ההודעה מוכנה לשליחה, כולל בקשה
לחזור עם הצעת מחיר. במקביל הסיכום נשלח גם לגיליון, בעמודת „הערות",
עם „· אשף" בעמודת המקור — כך שהליד לא נאבד גם אם הלקוח לא לחץ שליחה.

בדרך הלקוח יכול לשאול שאלות, והאשף עונה מתוך טבלת תשובות מוגדרת
(חומרים, תחזוקה, אחריות, תקנים, לוחות זמנים, ניקוז ועוד).

**חשוב להבין מה זה לא:** זה לא מודל שפה. דף סטטי לא יכול להחזיק מפתח
API בבטחה — הוא היה חשוף לכל מי שפותח „הצג מקור" — ולכן האשף הוא שאלון
מסועף עם טבלת תשובות. המשמעות המעשית: על שאלה שאין לה תשובה מוגדרת
הוא **אומר במפורש שהוא לא יודע ומעביר את השאלה בסיכום**, במקום לנחש.
הוא גם לא מוסר מחירים בשום מצב.

אם תרצו אשף שמבין כל שאלה חופשית, זה דורש מפתח API ושרת ביניים שיחזיק
אותו (אפשר לבנות אותו על אותו Apps Script). זה כבר כרוך בעלות לפי
שימוש. תגידו ואוסיף.

---

## נגישות

באתר יש כפתור נגישות קבוע בפינה, בכל שלושת העמודים, ובו: הגדלת טקסט
בארבע דרגות, ניגודיות גבוהה, גווני אפור, גופן קריא ומרווח, הדגשת
קישורים, עצירת אנימציות, סמן מוגדל וקו קריאה. ההעדפות נשמרות
ב-localStorage של הגולש. בנוסף: קישור „דילוג לתוכן", ניווט מקלדת מלא,
סימון מיקוד, מלכודת מיקוד בחלון האשף, טקסט חלופי לכל התמונות, תוויות
לכל שדה, והיררכיית כותרות תקנית.

ההנגשה נעשתה לפי ת״י 5568 ברמה AA, כנדרש בתקנות שוויון זכויות לאנשים
עם מוגבלות (התאמות נגישות לשירות), תשע״ג־2013.

> **שני דברים שחייבים להשלים:**
>
> 1. **פרטי רכז נגישות** — שם, טלפון ודואר אלקטרוני, ב-`partials/config.js`.
>    התקנות מחייבות שהצהרת הנגישות תכלול אותם. כל עוד הם ריקים, ההצהרה
>    מציגה „— להשלמה —" ואינה עומדת בדרישה.
> 2. **בדיקת נגישות** — האתר לא נבדק על ידי מוסמך נגישות ולא נבדק מול
>    קוראי מסך בפועל. בניתי לפי דרישות התקן, אבל אני לא יכול להעיד על
>    עמידה בהן. לעסק שחייב בהנגשה מלאה, בדיקה מקצועית היא הדבר שמגן.

---

## המסמכים המשפטיים

[`terms.html`](docs/terms.body.html) ו-[`accessibility.html`](docs/accessibility.body.html)
מקושרים בשורה דיסקרטית בתחתית הדף הראשי, מתחת לפרטי הקשר — לא בתפריט
ולא בכפתור.

תנאי השימוש קובעים שהאתר הוא אמצעי הצגה ויצירת קשר בלבד, שהמידע בו אינו
הצעה מחייבת, ושהביצוע והאחריות הם של הגורם המבצע שעימו הלקוח מתקשר
ישירות. יש בהם גם סעיף פרטיות שמפרט מה נאסף ולאן זה נשמר.

> **שני דברים שצריך לדעת:**
>
> 1. **זה לא ייעוץ משפטי.** כתבתי נוסח בסיס על בסיס תוכן האתר. עורך דין
>    צריך לעבור עליו, ולהשלים את פרטי העוסק (שם, ח.פ. או ע.מ., כתובת).
> 2. **יש מתח בין המסמך לדף.** הדף מצהיר „ייצור בנגרייה עצמית" ו„אחריות
>    5 שנים על השלד", והמסמך אומר שהאחריות היא של הגורם המבצע. אם אתם
>    עצמכם המבצעים — אין כאן בעיה, סעיף 4 פשוט מפנה ללקוח שמולכם הוא
>    מתקשר. אבל אם הכוונה היא להסיר מעצמכם אחריות על עבודה שאתם מבצעים,
>    זו סתירה: היא מחלישה את הסעיף ועלולה להיחשב הטעיה. סעיף שמנוסח
>    בבירור ומתאים למבנה העסקי בפועל מגן טוב יותר מסעיף גורף.

---

## תקשורת: וואטסאפ בלבד

אין בדף אף קישור `tel:` ואף אפשרות חיוג — לפי בקשה. כל דרכי הפנייה
מובילות לוואטסאפ (`055-927-2777`), ויש סעיף שלם בדף שמסביר ללקוח למה
זו הדרך הנכונה: הידיים על מכונה, תמונה חוסכת עשר שאלות, הכל נשאר כתוב,
ואפשר לכתוב בשעה שנוחה.

שדה הטלפון בטופס נשאר, אבל מתויג **„מספר וואטסאפ"** — כי זה המספר שאליו
תחזרו בהודעה.

### מה מופיע בכל הודעה

כל הודעה שנפתחת מהאתר מתחילה בשורת מקור, כך שכבר ברשימת השיחות רואים
מאיפה היא הגיעה:

```
📍 הגיע מאתר http://pergula.online/ · דף הבית · הכפתור הצף
```

החלק האחרון הוא הכפתור שנלחץ — הערך של `data-wa` על הקישור. כפתור
וואטסאפ חדש צריך לקבל ערך כזה, אחרת השורה תציין רק אתר ועמוד.

אם הלקוח כבר השאיר פרטים בטופס או סיים את האשף, ההודעה כוללת גם סיכום
שלהם (שם, מספר, כתובת, מה בונים, הערות ושאלות). זה עובד מכל כפתור
וואטסאפ באתר, גם בעמודי המסמכים, עד שהלקוח סוגר את הלשונית. בלי פרטים,
נשלח הנוסח הרגיל מ-`whatsappText`.

---

## תוכן שצריך לעבור עליו

הטקסטים נכתבו כדי להיות אמינים ומקצועיים, אבל **הם הנחות שלי, לא עובדות
שאומתו מולכם.** עברו על אלה ותקנו לפי המציאות שלכם:

- שם העסק **"עץ וצל"** — placeholder.
- אחריות **5 שנים על השלד** ו**שנתיים על הגימור**.
- זמני האספקה בשלבי התהליך (ייצור 2–4 שבועות, התקנה 1–3 ימים).
- **"ייצור בנגרייה עצמית"** ו**"מדידה ללא עלות"** — הצהרות שיווקיות מחייבות.
- כל המפרטים הטכניים (מידות שלבים, מרווחים, סוגי עוגנים, חומרים).

גובה מעקה 105 ס״מ ומרווח סורגים עד 10 ס״מ נלקחו מת״י 1142; ודאו מול היועץ
שלכם שזו הדרישה הרלוונטית לפרויקטים שאתם מבצעים בפועל.

### התמונות

התמונות ב-`img/` נחתכו מהקולאז'ים שהתקבלו, והן **לא צילומים של עבודות
שלכם**. לכן:

- הסעיף בדף נקרא **„סגנונות"** ולא „הפרויקטים שלנו", והכיתובים מתארים
  סגנון ולא פרויקט שבוצע. זה מכוון — אל תשנו את זה ל„עבודות שלנו"
  כל עוד אלה התמונות.
- ודאו שיש לכם זכויות שימוש בהן.
- ברגע שיש צילומים אמיתיים משלכם, החליפו את הקבצים ב-`img/` באותם
  שמות — הדף יתפוס אותם בלי שינוי קוד. אז גם אפשר לקרוא לסעיף
  „העבודות שלנו".

---

## מה הדף כן עושה

- RTL מלא, עברית, רספונסיבי ממסך רחב עד נייד.
- ערכת נושא בהירה וכהה לפי העדפת המערכת של הגולש.
- ולידציה בעברית: שם, מספר ישראלי (מקבל גם `+972` ומקפים), וכתובת.
- כפתור וואטסאפ צף, שהופך לסרגל תחתון בנייד.
- המספר נשמר בגיליון כטקסט, כך שה-0 המוביל לא נעלם.
- תצוגה מקדימה של קישור בוואטסאפ מראה את תמונת הפרגולה המקומרת.
